var expect    = require ('expect.js')
    , install = require ('../lib/commands/install');

describe('A installation', function () {
  it('should install the requested font without typers.json', function (done) {
    // TODO
    done();
  });
});
